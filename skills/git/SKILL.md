---
name: git
description: Use when committing, pushing, or performing any git/GitHub operation. Ensures correct repo detection, clean commit messages, and zero AI traces.
---

# Git

Commit and push without leaving AI fingerprints.

## Workflow

```dot
digraph git_flow {
    "User says push/commit" [shape=doublecircle];
    "Find .git for each changed file" [shape=box];
    "Multiple repos?" [shape=diamond];
    "Handle each repo separately" [shape=box];
    "Single repo" [shape=box];
    "Changes touch multiple areas?" [shape=diamond];
    "Ask what to include" [shape=box];
    "Can you name the project?" [shape=diamond];
    "Ask user" [shape=box];
    "Read git log --oneline -10" [shape=box];
    "Draft commit message in repo style" [shape=box];
    "Scrub AI traces from message AND diff" [shape=box];
    "Stage specific files, commit, push" [shape=box];
    "Done" [shape=doublecircle];

    "User says push/commit" -> "Find .git for each changed file";
    "Find .git for each changed file" -> "Multiple repos?";
    "Multiple repos?" -> "Handle each repo separately" [label="yes"];
    "Multiple repos?" -> "Single repo" [label="no"];
    "Handle each repo separately" -> "Changes touch multiple areas?";
    "Single repo" -> "Changes touch multiple areas?";
    "Changes touch multiple areas?" -> "Ask what to include" [label="yes"];
    "Changes touch multiple areas?" -> "Can you name the project?" [label="no"];
    "Ask what to include" -> "Can you name the project?";
    "Can you name the project?" -> "Ask user" [label="no"];
    "Can you name the project?" -> "Read git log --oneline -10" [label="yes"];
    "Ask user" -> "Read git log --oneline -10";
    "Read git log --oneline -10" -> "Draft commit message in repo style";
    "Draft commit message in repo style" -> "Scrub AI traces from message AND diff";
    "Scrub AI traces from message AND diff" -> "Stage specific files, commit, push";
    "Stage specific files, commit, push" -> "Done";
}
```

## Detect the Right Repo

Sub-projects may have their own `.git` (e.g. a website folder inside the vault). Before any git operation:

```bash
git -C <file-directory> rev-parse --show-toplevel
```

If changed files span multiple repos, handle each repo as a separate commit+push.

## Commit Message

Read `git log --oneline -10` from the target repo. Match the existing style exactly: tense, casing, format, length. Do not invent a new convention.

## Scrub AI Traces

Before committing, verify NONE of these appear in the commit message or staged diff:

- `Co-Authored-By: Claude` or any AI co-author line
- "generated by AI/Claude", "assisted by", "with help from"
- Em dashes (AI writing tell)
- Comments like `// AI-generated`, `<!-- Claude -->`, etc.

If any AI trace is found in the diff itself (e.g. a comment you wrote earlier in the session), remove it from the file before staging.

## Scope

- Never `git add -A` or `git add .`. Stage specific files by name.
- If changes touch unrelated areas, ask what to include in this commit.
- If you cannot identify which project the changes belong to, ask before committing.

## Safety

- Never force-push.
- Never amend published commits.
- Never push to a branch you did not create without confirming.
- Never skip hooks (no `--no-verify`).
